var playerName;
var locationName;
var score; // 1. Declare the score variable

playerName = "Lionel Messi";
locationName = "Argentina";
score = 100; // 2. Assign a value to score

console.log(playerName + " is in " + locationName);
console.log(playerName + " has a score of " + score); // 3. Display the message
