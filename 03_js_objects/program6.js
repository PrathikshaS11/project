var book;
var bookName;

bookName = "The Adventures of Tom Sawyer";

book = {
    title: bookName
};

// Logging the book object to the console
console.log(book);

// Changing the bookName variable
bookName = "Pride and Prejudice";

// Updating the book object
book = {
    title: bookName
};

// Logging the updated book object to the console
console.log(book);
