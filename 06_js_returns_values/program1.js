// Define getMessage function to return a message
var getMessage = function () {
    return "I’m going on an adventure!";
};

// Call getMessage and store the returned value in response
var response = getMessage();

// Output the response to the console
console.log(response);

/* Further Adventures */

// Define another function getMyMessage
var getMyMessage = function () {
    return "Hello from the function!";
};

// Now you can call getMyMessage at the console prompt
// Open your browser's developer tools (usually F12 or right-click -> Inspect -> Console)
// Then type getMyMessage() and press Enter
// You should see the returned message printed in the console
